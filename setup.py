from setuptools import setup, find_packages

setup(name='equivariance_smoothing',
      version='0.1.0',
      description='provable adversarial robustness for equivariant models',
      packages=find_packages(),
      zip_safe=False)
